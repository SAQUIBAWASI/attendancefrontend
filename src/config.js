// src/config.js

// By default, pointing to local backend. 
// Change this to "https://api.timelyhealth.in/api" when deploying to production.
export const API_BASE_URL = "http://localhost:5000/api";
